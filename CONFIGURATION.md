# GrillRadar 配置指南

> 🎯 **目标**: 让配置过程尽可能简单、灵活、友好

## 📋 目录

- [快速开始](#快速开始)
- [配置方法对比](#配置方法对比)
- [详细配置指南](#详细配置指南)
- [与其他项目对比](#与其他项目对比)
- [常见问题](#常见问题)
- [故障排除](#故障排除)

---

## 🚀 快速开始

### 方法 1: 交互式配置向导 (推荐新手)

最简单的方式，5 分钟完成配置：

```bash
python setup_config.py
```

向导会引导你完成：
- ✅ 选择 LLM 提供商（Anthropic/OpenAI）
- ✅ 配置 API 密钥
- ✅ 选择模型和参数
- ✅ 自动生成 .env 文件
- ✅ 验证配置正确性

**这是 GrillRadar 独有的特性！其他项目都需要手动配置。**

### 方法 2: 快速复制配置 (推荐熟手)

如果你熟悉环境变量配置：

```bash
# 复制模板
cp .env.example .env

# 编辑配置文件
nano .env  # 或使用你喜欢的编辑器

# 修改以下关键配置:
# ANTHROPIC_API_KEY=your_api_key_here
```

### 方法 3: 使用环境变量 (推荐生产环境)

直接设置环境变量，无需配置文件：

```bash
export ANTHROPIC_API_KEY=your_api_key_here
export DEFAULT_LLM_PROVIDER=anthropic
export DEFAULT_MODEL=claude-sonnet-4

# 启动应用
python -m uvicorn app.main:app
```

---

## 🔧 配置方法对比

| 方法 | 难度 | 适用场景 | 优势 |
|------|------|----------|------|
| **交互式向导** | ⭐ 非常简单 | 新手、首次配置 | 引导式配置，零门槛 |
| **复制模板** | ⭐⭐ 简单 | 熟悉配置文件 | 快速、可重复使用 |
| **环境变量** | ⭐⭐⭐ 中等 | 生产部署、Docker | 灵活、安全、易于CI/CD |
| **YAML配置** (未来) | ⭐⭐ 简单 | 团队协作 | 结构化、易于版本控制 |

---

## 📖 详细配置指南

### 1. LLM 提供商配置

GrillRadar 支持多种 LLM 提供商，选择其中一种即可：

#### Option A: Anthropic Claude (推荐)

**为什么推荐?**
- ✅ 强大的推理和分析能力
- ✅ 支持超长上下文 (200K tokens)
- ✅ 优秀的中文支持
- ✅ 更安全的内容过滤

**配置方式:**

```env
# 官方 API
ANTHROPIC_API_KEY=sk-ant-your-api-key-here
DEFAULT_LLM_PROVIDER=anthropic
DEFAULT_MODEL=claude-sonnet-4
```

**获取 API Key:**
1. 访问 https://console.anthropic.com/
2. 注册账号
3. 导航到 API Keys
4. 创建新的 API Key
5. 复制并粘贴到配置文件

#### Option B: 第三方 Anthropic 兼容服务

**适用场景:**
- 🌏 国内访问受限
- 🏢 企业内部 API 网关
- 💰 使用国内服务商

**示例: BigModel (智谱 AI)**

```env
ANTHROPIC_AUTH_TOKEN=your_token_here
ANTHROPIC_BASE_URL=https://open.bigmodel.cn/api/anthropic
DEFAULT_LLM_PROVIDER=anthropic
DEFAULT_MODEL=claude-sonnet-4
```

**获取方式:**
1. 访问 https://open.bigmodel.cn/
2. 注册并实名认证
3. 创建 API Key
4. 选择 "Anthropic 兼容" 接口
5. 复制 Token 和 Base URL

#### Option C: OpenAI GPT

**优势:**
- ✅ 生态丰富
- ✅ 多模型选择
- ✅ 工具链成熟

**配置方式:**

```env
OPENAI_API_KEY=sk-your-openai-key-here
DEFAULT_LLM_PROVIDER=openai
DEFAULT_MODEL=gpt-4o
```

**获取 API Key:**
1. 访问 https://platform.openai.com/
2. 注册账号
3. 导航到 API Keys: https://platform.openai.com/api-keys
4. 创建新的 Secret Key
5. 复制并保存（只显示一次）

### 2. 模型选择指南

#### Anthropic 模型对比

| 模型 | 性能 | 成本 | 适用场景 |
|------|------|------|----------|
| **claude-sonnet-4** | ⭐⭐⭐⭐ | $$ | **推荐** - 平衡性能和成本 |
| **claude-opus-4** | ⭐⭐⭐⭐⭐ | $$$ | 最强推理，复杂分析 |
| **claude-3-5-sonnet** | ⭐⭐⭐⭐ | $$ | Legacy 版本 |

**推荐配置:**

```env
# 标准配置 (推荐)
DEFAULT_MODEL=claude-sonnet-4

# 高性能配置 (复杂场景)
DEFAULT_MODEL=claude-opus-4
```

#### OpenAI 模型对比

| 模型 | 性能 | 成本 | 适用场景 |
|------|------|------|----------|
| **gpt-4o** | ⭐⭐⭐⭐⭐ | $$$ | **推荐** - 最新最强 |
| **gpt-4-turbo** | ⭐⭐⭐⭐ | $$ | 平衡选择 |
| **gpt-3.5-turbo** | ⭐⭐⭐ | $ | 经济型 |

### 3. 参数调优

#### Temperature (创造性控制)

控制输出的随机性和创造性：

```env
# 低温度: 一致性高，适合标准化面试
LLM_TEMPERATURE=0.3

# 中温度: 平衡创造性和一致性 (推荐)
LLM_TEMPERATURE=0.7

# 高温度: 更多样化的问题
LLM_TEMPERATURE=0.9
```

**推荐值:**
- 技术面试准备: 0.5-0.7
- 行为面试准备: 0.7-0.8
- 研究生面试: 0.6-0.7

#### Max Tokens (生成长度)

控制生成报告的详细程度：

```env
# 简短报告 (5-8个问题)
LLM_MAX_TOKENS=8000

# 标准报告 (10-15个问题) - 推荐
LLM_MAX_TOKENS=16000

# 详细报告 (15-20个问题)
LLM_MAX_TOKENS=32000
```

**注意:** 更大的值会增加 API 成本

#### Timeout (超时时间)

```env
# 快速响应 (60秒)
LLM_TIMEOUT=60

# 标准配置 (120秒) - 推荐
LLM_TIMEOUT=120

# 复杂报告 (5分钟)
LLM_TIMEOUT=300
```

---

## 🆚 与其他项目对比

### 配置方式对比

| 特性 | GrillRadar | TrendRadar | BettaFish |
|------|------------|------------|-----------|
| **交互式配置向导** | ✅ 有 | ❌ 无 | ❌ 无 |
| **环境变量支持** | ✅ 完整 | ✅ 基础 | ✅ 完整 |
| **.env 文件** | ✅ 有详细模板 | ✅ 简单模板 | ✅ 有 |
| **配置验证** | ✅ 启动时自动验证 | ⚠️ 部分验证 | ✅ 有 |
| **错误提示** | ✅ 详细中英文提示 | ⚠️ 简单提示 | ✅ 英文提示 |
| **YAML 配置** | 🔜 计划中 | ❌ 无 | ✅ 有 |
| **配置测试工具** | ✅ `--test` 参数 | ❌ 无 | ⚠️ 需手动 |

### 用户体验对比

#### TrendRadar
```bash
# 步骤多，容易出错
cp .env.example .env
vi .env  # 手动编辑
# 启动后才知道配置是否正确
```

#### BettaFish
```bash
# 需要同时配置多个文件
cp .env.example .env
cp config.example.yaml config.yaml
vi .env  # 编辑环境变量
vi config.yaml  # 编辑配置
```

#### GrillRadar (本项目)
```bash
# 一条命令，交互式完成
python setup_config.py
# ✅ 自动生成 .env
# ✅ 自动验证配置
# ✅ 提供下一步指引
```

### 配置灵活性对比

| 需求 | GrillRadar | TrendRadar | BettaFish |
|------|------------|------------|-----------|
| 新手首次配置 | ⭐⭐⭐⭐⭐ 交互式向导 | ⭐⭐ 手动编辑 | ⭐⭐⭐ 文档引导 |
| 生产环境部署 | ⭐⭐⭐⭐⭐ 环境变量 | ⭐⭐⭐⭐ 环境变量 | ⭐⭐⭐⭐⭐ 多种方式 |
| 团队协作 | ⭐⭐⭐⭐ 模板 | ⭐⭐⭐ 模板 | ⭐⭐⭐⭐⭐ YAML |
| Docker 部署 | ⭐⭐⭐⭐⭐ 环境变量 | ⭐⭐⭐⭐ 环境变量 | ⭐⭐⭐⭐ 环境变量 |
| 快速测试 | ⭐⭐⭐⭐⭐ `--test` | ⭐⭐ 手动测试 | ⭐⭐⭐ 命令行 |

---

## ❓ 常见问题

### Q1: 我应该使用哪个 LLM 提供商？

**推荐:** Anthropic Claude

**原因:**
- ✅ 更强的推理能力（对复杂简历分析更准确）
- ✅ 更长的上下文窗口（200K vs 128K）
- ✅ 更好的中文支持
- ✅ 更安全的内容过滤

**何时使用 OpenAI:**
- 已有 OpenAI API 订阅
- 需要使用 OpenAI 生态工具
- GPT-4o 在特定场景表现更好

### Q2: 配置文件在哪里？

配置文件的查找顺序：

1. **当前目录的 .env 文件** (最高优先级)
2. **环境变量** (覆盖 .env)
3. **默认值** (app/config/settings.py)

```bash
# 检查当前配置
python setup_config.py --test
```

### Q3: 如何验证配置是否正确？

```bash
# 方法 1: 使用配置测试工具
python setup_config.py --test

# 方法 2: 启动应用 (会自动验证)
python -m uvicorn app.main:app --reload

# 方法 3: 访问配置状态端点
curl http://localhost:8000/api/config/status
```

### Q4: 可以不配置 API Key 直接使用吗？

❌ **不可以**

GrillRadar 需要调用 LLM API 来生成面试问题，必须配置有效的 API Key。

**替代方案:**
- 使用第三方兼容服务（如 BigModel，国内访问更稳定）
- 使用免费额度（Anthropic 和 OpenAI 都提供新用户免费额度）

### Q5: 配置文件可以提交到 Git 吗？

⚠️ **不推荐**

.env 文件包含敏感信息（API Key），不应提交到版本控制。

**最佳实践:**
```bash
# .gitignore 中已包含
.env
.env.local
.env.production

# 可以提交模板
.env.example ✅
.env.example.detailed ✅
```

### Q6: 如何在 Docker 中配置？

**方法 1: 使用环境变量** (推荐)

```bash
docker run -e ANTHROPIC_API_KEY=xxx \
           -e DEFAULT_LLM_PROVIDER=anthropic \
           grillradar:latest
```

**方法 2: 使用 .env 文件**

```bash
docker run --env-file .env grillradar:latest
```

**方法 3: docker-compose**

```yaml
services:
  grillradar:
    image: grillradar:latest
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - DEFAULT_LLM_PROVIDER=anthropic
    # 或
    env_file:
      - .env
```

---

## 🔧 故障排除

### 问题 1: "ANTHROPIC_API_KEY not set"

**原因:** 未配置 API 密钥

**解决方案:**
```bash
# 方法 1: 运行配置向导
python setup_config.py

# 方法 2: 手动编辑 .env
echo "ANTHROPIC_API_KEY=your_key_here" >> .env

# 方法 3: 设置环境变量
export ANTHROPIC_API_KEY=your_key_here
```

### 问题 2: "Configuration validation failed"

**原因:** 配置文件格式错误或值无效

**解决方案:**
```bash
# 1. 检查配置
python setup_config.py --test

# 2. 重新运行向导
python setup_config.py

# 3. 查看详细错误
python -m uvicorn app.main:app --log-level debug
```

### 问题 3: "Module 'anthropic' not found"

**原因:** 未安装依赖

**解决方案:**
```bash
# 安装所有依赖
pip install -r requirements.txt

# 或仅安装 Anthropic
pip install anthropic

# 或仅安装 OpenAI
pip install openai
```

### 问题 4: API 调用超时

**原因:** 网络问题或超时时间设置过短

**解决方案:**
```env
# 增加超时时间
LLM_TIMEOUT=300  # 5 分钟

# 或使用国内第三方服务
ANTHROPIC_BASE_URL=https://open.bigmodel.cn/api/anthropic
```

### 问题 5: 配置修改不生效

**原因:** 未重启应用或环境变量优先级

**解决方案:**
```bash
# 1. 重启应用
# Ctrl+C 停止
python -m uvicorn app.main:app --reload

# 2. 检查环境变量
env | grep ANTHROPIC

# 3. 清除可能的缓存
unset ANTHROPIC_API_KEY  # 清除环境变量
rm .env  # 删除旧配置
python setup_config.py  # 重新配置
```

---

## 📚 进阶配置

### 使用配置文件管理多环境

```bash
# 开发环境
cp .env.development .env

# 测试环境
cp .env.testing .env

# 生产环境
cp .env.production .env
```

### 配置优先级详解

```
环境变量 (最高)
    ↓
.env 文件
    ↓
默认值 (最低)
```

示例:
```bash
# .env 文件
DEFAULT_MODEL=claude-sonnet-4

# 环境变量 (会覆盖 .env)
export DEFAULT_MODEL=claude-opus-4

# 实际使用: claude-opus-4
```

---

## 🎯 最佳实践

### 1. 新项目配置清单

- [x] 运行 `python setup_config.py` 配置向导
- [x] 测试配置 `python setup_config.py --test`
- [x] 将 .env 添加到 .gitignore
- [x] 创建 .env.example 供团队使用
- [x] 记录使用的模型和参数

### 2. 团队协作配置

```bash
# 1. 提交配置模板
git add .env.example
git commit -m "Add environment config template"

# 2. 团队成员各自配置
cp .env.example .env
# 编辑自己的 .env 文件

# 3. 保持模板更新
# 当添加新配置项时，更新 .env.example
```

### 3. 生产环境配置

```bash
# 使用环境变量，不使用 .env 文件
export ANTHROPIC_API_KEY=xxx
export DEFAULT_LLM_PROVIDER=anthropic
export DEBUG=False

# 或使用密钥管理服务
# AWS Secrets Manager, Azure Key Vault, etc.
```

---

## 📞 获取帮助

如果配置过程中遇到问题：

1. **查看文档**
   - [完整 README](./README.md)
   - [API 文档](http://localhost:8000/docs) (启动后)

2. **运行诊断**
   ```bash
   python setup_config.py --test
   ```

3. **查看日志**
   ```bash
   python -m uvicorn app.main:app --log-level debug
   ```

4. **提交 Issue**
   - GitHub Issues: [项目链接]
   - 附上配置测试输出和错误日志

---

## 🚀 快速参考

### 三种最常见的配置方式

#### 1. 零配置开始 (使用向导)
```bash
python setup_config.py
# 按提示操作，5分钟完成
```

#### 2. 快速复制配置
```bash
cp .env.example .env
nano .env  # 修改 ANTHROPIC_API_KEY
python -m uvicorn app.main:app --reload
```

#### 3. 纯环境变量
```bash
export ANTHROPIC_API_KEY=your_key
export DEFAULT_LLM_PROVIDER=anthropic
python -m uvicorn app.main:app
```

---

**配置愉快！🎉**
